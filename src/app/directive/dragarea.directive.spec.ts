import { DragareaDirective } from './dragarea.directive';

describe('DragareaDirective', () => {
  it('should create an instance', () => {
    const directive = new DragareaDirective();
    expect(directive).toBeTruthy();
  });
});
